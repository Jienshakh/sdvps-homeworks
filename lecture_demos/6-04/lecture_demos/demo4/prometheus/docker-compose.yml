version: '3.8'

volumes:
  prometheus_data: {}
  grafana_data: {}

networks:
  odinaevdd-my-netology-hw:
    driver: bridge
    ipam:
      config:
        - subnet: "10.5.0.0/16"

services:

  prometheus:
    image: prom/prometheus:v2.36.2
    container_name: odinaevdd-netology-prometheus
    volumes:
      - ./prometheus/:/etc/prometheus/
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    ports:
      - 9090:9090
    links:
#      - odinaevdd-netology-cadvisor:cadvisor
#      - odinaevdd-netology-alertmanager:alertmanager
      - pushgateway:pushgateway
    depends_on:
#      - odinaevdd-netology-cadvisor 
      - pushgateway 
    networks:
      - odinaevdd-my-netology-hw
    restart: always
#    deploy:
#      placement:
#        constraints:
#          - node.hostname == ${HOSTNAME}

#  odinaevdd-netology-alertmanager:
#    image: prom/alertmanager
#    ports:
#      - 9093:9093
#    volumes:
#      - ./alertmanager/:/etc/alertmanager/
#    networks:
#      - odinaevdd-my-netology-hw
#    restart: always
#    command:
#      - '--config.file=/etc/alertmanager/config.yml'
#      - '--storage.path=/alertmanager'
#    deploy:
#      placement:
#        constraints:
#          - node.hostname == ${HOSTNAME}


#  odinaevdd-netology-cadvisor:
#    image: gcr.io/cadvisor/cadvisor
#    volumes:
#      - /:/rootfs:ro,rslave
#      - /var/run:/var/run:rw
#      - /sys:/sys:ro
#      - /var/lib/docker/:/var/lib/docker:ro,rslave
#    ports:
#      - 8080:8080
#    networks:
#      - odinaevdd-my-netology-hw
#    restart: always
#    deploy:
#      mode: global


  pushgateway:
    image: prom/pushgateway
    container_name: odinaevdd-netology-pushgateway
    restart: always
    expose:
      - 9091
    ports:
      - "9091:9091"
    networks:
      - odinaevdd-my-netology-hw

  grafana:
    image: grafana/grafana
    container_name: odinaevdd-netology-grafana
    user: "472"
    depends_on:
      - prometheus
    ports:
      - 80:3000
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning/:/etc/grafana/provisioning/
    env_file:
      - ./grafana/config.monitoring
    networks:
      - odinaevdd-my-netology-hw
    restart: always
